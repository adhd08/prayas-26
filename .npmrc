# CI validates lockfiles with default npm config. A contributor whose personal
# ~/.npmrc sets legacy-peer-deps=true resolves peer dependencies differently,
# producing a lockfile that `npm ci` rejects. Pin it so every install matches
# what the runner expects. frontend/ carries its own copy, since npm reads the
# project config from the prefix directory rather than the repo root.
legacy-peer-deps=false

# CI runs `npm ci` with default npm config. A contributor whose personal
# ~/.npmrc sets legacy-peer-deps=true generates a lockfile with peer
# dependencies omitted, which then fails `npm ci` in CI. Pin it here so the
# lockfile is always resolved the same way the runner validates it.
legacy-peer-deps=false
